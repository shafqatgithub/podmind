# PodMind AI — API runtime dependencies (Python 3.11+)
# Pin major.minor; patch releases float for security fixes.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
email-validator>=2.2,<3.0

asyncpg>=0.29,<1.0

structlog>=24.4,<26.0
httpx>=0.27,<1.0

# Auth: Supabase JWT verification (RS256/ES256 via JWKS)
PyJWT[crypto]>=2.9,<3.0
# Fernet encryption for user BYOK provider keys
cryptography>=43.0
