# Deploy batches
Auto-generated from `docs/database/*.sql` (source of truth): comments and
transaction statements stripped, content otherwise identical. Applied to the
live database in order (cbatch1 → cbatch8). Regenerate whenever docs/database changes.
